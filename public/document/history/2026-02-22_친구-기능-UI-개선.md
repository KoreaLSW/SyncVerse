# 2026-02-22 작업 기록 (2)

## 인증/세션

- NextAuth 세션 유지기간을 7일로 설정

## 메인 화면 UI 개선

- Online Users 패널과 Live Chat 패널에 확대/축소(ON/OFF) 토글 추가
    - Online Users: OFF 시 인원수만 표시
    - Live Chat: OFF 시 최신 메시지 한 줄만 표시, 입력창은 유지
- Online Users 패널을 `OnlineUsersPanel` 컴포넌트로 분리

## 캐릭터 상호작용 UI

- 캐릭터 우클릭 시 컨텍스트 메뉴 표시
    - 친구 추가 / 차단 / 1:1 대화 걸기 항목 제공
- 내 캐릭터에는 우클릭 메뉴가 뜨지 않도록 처리
- 캐릭터 호버 시 “우클릭” 안내 배지 표시
- 캐릭터 위 커서 모양을 포인터로 변경

## 친구 기능 구현

- 친구 요청/수락 흐름 구현
    - 요청 생성 시 `friendships` 테이블에 `PENDING`으로 저장
    - 수락 시 `ACCEPTED`로 업데이트
- 요청 취소 기능 추가 (PENDING_SENT 해제)
- 친구 해제 기능 추가 (ACCEPTED 해제)
- 친구 상태별 버튼 라벨 변경
    - 친구추가 / 요청취소 / 수락하기 / 친구해제

## 친구 상태 표시 (배지)

- 캐릭터 닉네임 영역에 친구 배지 표시
- 채팅 메시지 닉네임 옆에 친구 배지 표시
- 화이트보드 원격 커서 닉네임 옆에 친구 배지 표시

## 전역 친구 캐시 + 실시간 반영

- 친구 목록을 전역 store로 관리 (Zustand)
- 친구 목록 API 추가 (`/api/friends/list`)
- Supabase Realtime 구독으로 친구 상태 변경 즉시 반영
- 친구 액션 결과를 전역 캐시에 즉시 반영
